<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title></title>

    @stack('css')

</head>
<body>
<main>
    @include('{{ namespace_view }}.header')

    @yield('content')

    @include('{{ namespace_view }}.footer')
</main>

</body>
</html>

